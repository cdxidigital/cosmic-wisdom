# Runtime verification notes

- 2026-08-27: The Compatibility route initially rendered its signed-out gate while the authenticated session was resolving. A direct authenticated session request returned the current member successfully, so the next check is whether the profile query completes and rerenders the gate before changing member-access logic.
- 2026-08-27: The Compatibility profile query completed and enabled the form without a code change. Steven Edgintonm and 1991-06-10 were entered only into the active browser form after explicit consent; no profile, reading, or file mutation was invoked.
- 2026-08-27: The private natal response confirms the member has a calculated Aquarius Sun. The enhanced reflection will use that verified owned-chart input and Steven’s date-derived Gemini Sun only; it will not derive Steven’s Moon, Rising, houses, aspects, or full synastry without his birth time.
- 2026-08-27: The enhanced form accepts Yanchep, Western Australia as a browser-local optional location note and labels it as unused without a birth time. Its wording explicitly rules out creation of a partner profile, reading-history entry, or file record.
- 2026-08-27: End-to-end browser validation passed for the consented member profile plus Steven Edgintonm, born 1991-06-10. The rendered result uses Aquarius + Gemini, provides four tailored reflection dimensions, and visibly excludes Moon, Rising, houses, aspects, and full synastry. The location note correctly confirms Yanchep does not alter this date-based result.
- 2026-08-27: A source and runtime audit found no mutation, file-storage, registration, upload, or save call in Compatibility. The requests recorded for the current compatibility render are limited to auth.me, cosmic.getMyProfile, and cosmic.getNatalChart reads; opening the lens is local React state only.
- 2026-08-27: First homepage validation pass: the runtime title is “Cosmic Wisdom: Astrology, Tarot & Numerology”; the authenticated state settles successfully and shows the member profile plus calculated Sun, Moon, Rising, natal reading, and all five primary lens links. The brief pre-query profile placeholder is replaced once the member queries complete.
- 2026-08-27: Linked-route validation: the homepage Numerology link resolved to a member-aware Life Path and Personal Year screen, then its Tarot link resolved to the three-position studio with placement-guide, draw, reset, and private-save controls. No runtime errors were visible in either screen.
- 2026-08-27: Tarot interaction validation: “Draw a card” correctly revealed The Hermit in the Context position with its reflective prompt; “Reset” returned all three positions to the unrevealed state. No private reading was saved during this test.
- 2026-08-27: Palmistry route validation: the Tarot-to-Palmistry link resolved correctly. The beta page presented its explicit on-device camera consent, no-upload/no-retention constraints, and four non-camera reflective line prompts. Selecting Heart Line updated its local visual focus without saving a reflection or requesting camera access.
- 2026-08-27: Independent Compatibility validation: the loading state clearly displayed while the private natal profile was checked, then cleared to enable the consent-gated date-based form. The private browser-page retention and no-persistence disclosures were present and legible.
- 2026-08-27: Natal-details validation: after the member chart query completed, the private method-notes page displayed 13 placements, 12 houses, 8 aspects, and an expandable Calculation Notes control. Its temporary loading state correctly resolved without an error boundary or broken route.
- 2026-08-27: The Calculation Notes disclosure opened and revealed its full method and limitation copy. The Daily Ritual route then loaded correctly with an optional, browser-only “Mark as noticed” action and an explicit no-streak/no-account-save explanation.
- 2026-08-27: Daily Ritual validation: “Mark as noticed” changed the browser-local control to “Noted for myself,” then its header link returned successfully to the homepage. No persistence action was invoked.
- 2026-08-27: Clean-restart verification: after restarting the service, the member session rehydrated, the private profile and calculated natal summary loaded, and the refreshed 44-character runtime title remained present. No active runtime fault appeared during the post-restart load.
- 2026-08-27: Final browser audit: the console is clear of client-side errors, and direct fetches to /, /numerology, /tarot, /palmistry, /compatibility, /natal-details, and /daily-ritual all returned HTTP 200 from the restarted service.
- 2026-08-27: Final homepage readiness check: the authenticated profile and natal summary again settled correctly after a fresh direct navigation, leaving the Compatibility call to action enabled from the intended home entry point.
- 2026-08-27: Homepage Compatibility navigation validation: the primary Compatibility card was scrolled into view and then activated successfully, routing to the private Compatibility page and showing its explicit profile-check loading state before form access.
- 2026-08-27: Final Palmistry recovery setup: the recovered beta page loaded after the clean restart and required an explicit on-device-only consent acknowledgement before its camera control could be tested. No camera request, image capture, upload, or save action has been made at this stage.
- 2026-08-27: Final Palmistry recovery result: the consented camera request was not granted, and the page safely showed “Your reflection does not depend on camera access,” plus retry and no-camera continuation controls. Selecting “Use no-camera prompts” returned focus to the reflective line-map experience. No image was captured, uploaded, retained, or saved.
- 2026-08-27: Live mobile navigation validation: an isolated real Chromium target at 375px opened the mobile home menu, found the visible Compatibility link, and followed it to /compatibility. The destination retained its private-data non-persistence disclosure and consent checkbox. The isolated target reported no runtime exceptions or error-level console messages.
- 2026-08-27: Final natal-details validation: after the temporary mobile harness restored the viewport, the authenticated browser again showed 13 placements, 12 houses, 8 aspects, and successfully expanded the native Calculation Notes disclosure. The page remained free of runtime faults.
- 2026-08-27: Responsive natal-details validation: the page rendered in the 375px route sweep, while an authenticated independent Chromium session at the responsive mobile breakpoint loaded its 13/12/8 counts and opened the native Calculation Notes disclosure. The effective Chromium layout width was 500px because of the browser’s headless minimum, which remains below the 768px mobile breakpoint; no runtime errors occurred.
- 2026-08-27: Explicit disclosure-state proof: the authenticated desktop browser returned `{ detailsFound: true, open: true, notesVisible: true }` for `/natal-details`. An authenticated independent Chromium check, with an exact 375px device viewport, also returned `summaryFound: true`, `open: true`, `width: 375`, and `methodTextVisible: true`, with no exception or error-level console events. Its process reported a delayed temporary-profile cleanup error after the assertions passed; the temporary browser profile was then removed successfully in a separate cleanup step.
- 2026-08-27: Final compact-menu evidence: an exact 375px browser device viewport successfully opened the home menu and followed Compatibility. A separate non-emulated Chromium/Xvfb run measured its native minimum content width as 500px (still below the 768px compact-menu breakpoint), opened the actual compact home menu, and followed visible Tarot, Palmistry, and Compatibility links to their fully loaded destination pages. Three destination screenshots were captured locally, and the run reported no runtime exceptions or error-level console messages. All temporary browser processes and profiles were removed afterwards.
- 2026-08-27: Renewed homepage SEO check: after client rendering, the live title is “Cosmic Wisdom: Private Astrology, Tarot and Numerology”; the actual home screen contains its visible descriptive H2, and the raw document also contains a semantic static fallback H2 for non-JavaScript scanners. React replaces that fallback cleanly without duplicate visible content.
- 2026-08-27: Exact renewed SEO measurements: title “Cosmic Wisdom: Private Astrology, Tarot and Numerology” is 54 characters; the description is 158 characters; the keyword tag contains 6 focused terms; and the static descriptive H2 is 64 characters. The type check, SEO test, full test suite (13 files / 33 tests), production build, and live browser render all passed.
- 2026-08-27: Exact compact-menu completion: at a 375px device viewport, the live home compact menu exposed visible Tarot, Palmistry, and Compatibility links. Each followed to its fully loaded destination at the same 375px width; all three destinations were captured, with no runtime exception or error-level console event. Temporary browser profiles were removed after the run.
- 2026-08-27: Exact mobile Tarot verification: at a 375px device viewport, three no-save draws produced Page of Wands, The Emperor, and The Magician, all distinct. The live reserve count reached 75, the Context / Threshold / Orientation synthesis was visible, and New Shuffle restored zero visible cards with 78 held in reserve. The test recorded no runtime error and did not invoke the save action; its temporary browser profile was removed afterwards.
- 2026-08-27: In-depth natal-pattern validation: the authenticated `/natal-details` view loaded 13 placements, 12 houses, and 8 aspects, then rendered the private pattern reading with Aquarius / Aries / Libra core axis, Earth emphasis, 3rd-house focus, and an aspect-integration prompt. The screen did not display raw birth date, time, or place and includes the linked on-demand daily practice.
- 2026-08-27: Daily teaching route validation: the authenticated `/daily-ritual` response derives a private Earth-emphasis teaching from the saved Aquarius / Aries / Libra chart pattern and the calendar date. It explicitly excludes transit forecasting, predictions, prompts, scoring, persistence, and new data transmission.
- 2026-08-27: Daily teaching interaction: the “Mark as noticed” control transitioned to “Noted for myself” in the active browser without a save action, supporting the stated browser-only and no-streak behaviour.
- 2026-08-27: Expanded astrology validation: component tests cover unauthenticated, authenticated-no-chart, and authenticated-chart-present states for both Natal Details and Daily Ritual. An isolated authenticated 375px browser followed natal pattern → daily teaching, recorded no raw ISO date/time in output, toggled the acknowledgement, made no save call, and produced no runtime errors. The public homepage raw shell and enriched client source contained zero raw date/time/location matches; both chart queries are explicitly gated by authentication.
- 2026-08-27: Local account validation: the additive `users.passwordHash` and unique-email migration was reviewed and applied. Unit coverage verifies normalized email, strong-password validation, salted scrypt hashing, constant-time verification, duplicate registration rejection, generic invalid-credential errors, five-attempt rate limiting, standard secure session-cookie issuance, and logout’s existing cookie clearing. An isolated 375px no-session browser confirmed Home’s profile call-to-action reaches `/account`, shows native email/password input types, and switches to registration with name and `new-password` controls without submitting credentials.
- 2026-08-27: Consent-approved full local-account lifecycle: an isolated exact-375px browser registered a disposable account, confirmed the shared member session through the mobile Sign Out control, opened the protected chart-details route and received the appropriate no-chart private state, signed out with the explicit menu control, signed back in with the same password, and reconfirmed the authenticated Sign Out state. No runtime errors occurred. The disposable account and temporary browser profile were immediately removed. The final type check, 20-file / 52-test suite, and production build passed.
- 2026-08-27: Independent desktop local-account validation: at 1280px, a consent-approved disposable email account completed registration, exposed the desktop Sign Out control, reached the protected no-chart member state, signed out using the visible desktop control, returned to the local sign-in form, signed back in, and again exposed the authenticated Sign Out state. No runtime errors occurred; the account and temporary test browser profile were removed immediately. The post-validation type check, 20 test files / 52 tests, and production build passed.
- 2026-08-27: Account Settings unauthenticated verification: direct navigation to `/account/settings` without a member session redirected to `/account` and rendered the local Sign In/Create Account interface. No private account data or destructive control was visible.
- 2026-08-27: Consent-approved Account Settings lifecycle: an isolated 375px member account registered successfully, reached `/account/settings`, changed its password, signed out, signed back in using the new password, and submitted the required current password plus `DELETE MY ACCOUNT` phrase. The application deleted the account, cleared its session, and displayed the private natal access gate on a subsequent `/natal-details` request with no Calculation Notes exposed. Runtime exceptions: 0. A database verification immediately after the run confirmed zero remaining disposable `qa.settings` accounts.
- 2026-08-27: Fresh audit visual sweep: desktop and 375px layouts render the core home, Tarot, Palmistry, Numerology, Compatibility, Natal Details, and Daily Ritual routes with no visible clipping. The Account mobile capture failed and requires a direct retry. Compatibility’s mobile screenshot was captured during its intentional profile-checking state and Daily Ritual’s desktop capture during its intentional chart-loading state; both require a settled-state check rather than being treated as defects. The visual language is coherent, though source/index metadata and accent usage should be reviewed during polish.
- 2026-08-27: Account audit follow-up: direct navigation to `/account` initially rendered the public email/password entry form correctly, with semantic email and password fields, password-hash privacy copy, and connected-account entry. Once the existing authenticated member query settled, the route intentionally returned to Home; the home remained fully rendered with profile, natal, and Sign Out controls. This resolves the failed screenshot as transient capture timing rather than an Account-page defect.
- 2026-08-27: Daily Ritual audit follow-up: the chart-loading state resolved within the browser to a private daily teaching with one practice, one pattern question, browser-only acknowledgement control, and explicit no-persistence/no-prediction language. No visual overflow or route failure was observed.
- 2026-08-27: Current runtime audit: the active browser console was clean. Browser-context checks returned HTTP 200 for Home, Account, Account Settings, Tarot, Tarot placement, Palmistry, Numerology, Compatibility, Natal Details, and Daily Ritual. The earlier log entries therefore remain historical sign-out/session-transition events rather than current runtime failures.
- 2026-08-27: Published homepage SEO audit: runtime title is 54 characters; description is 158 characters; keyword tag contains 6 focused terms; rendered H2s are 16, 23, and 42 characters; and all 3 visible homepage images have non-empty alternative text. The raw template also includes the 64-character static fallback H2. The reported scanner findings are not reproducible against the current published page.
- 2026-08-27: Audit route follow-up: `/tarot/placement` renders cleanly at desktop with an explicit 78-card reserve, clear non-repeating spread positions, and the physical placement guide. In contrast, direct signed-out access to published `/account/settings` remained on “OPENING PRIVATE SETTINGS…” rather than reaching a local-account entry state; this is a confirmed access-gate defect requiring repair.
- 2026-08-27: Account Settings diagnostic correction: the signed-out public `auth.me` request returned HTTP 200 with a null session. After the route’s normal loading transition, `/account/settings` redirected to `/account` and rendered the local Account interface. The initial loading observation was capture timing, not an indefinite access-gate defect.
- 2026-08-27: Updated home verification: the development homepage shows the aligned COSMIC WISDOM footer wordmark. In an authenticated session, the Natal Chart header control correctly scrolls to the calculated Big Three and reflective reading rather than opening an unintended route or modal.
- 2026-08-27: Final audit build: all 10 registered routes returned HTTP 200; the current console remained clean; desktop and 375px route sweeps revealed no clipped primary content; strict homepage SEO values remain title 54 characters, description 158 characters, 6 keywords, static H2 64 characters, and 3 non-empty image alternatives. Secondary pages are now route-lazy-loaded, reducing the main app chunk from approximately 801 kB to 66 kB (gzip 12.5 kB); the largest shared runtime chunk is 400 kB. Final validation passed type checking, 55 tests across 20 passing files (one opt-in live credential probe skipped), and the production build.
- 2026-08-27: Live Tarot deck verification: a newly loaded spread reported 78 cards held in reserve. Three successive no-save draws produced two distinct Pentacles cards and Ten of Cups, with the reserve count decrementing 78 → 77 → 76 → 75 and a completed Context / Threshold / Orientation synthesis. “New Shuffle” restored an empty spread and 78 cards held in reserve without persisting a reading.
- 2026-08-27: Personalisation verification: the navigation theme control switched to dark (`documentElement.dark`, persisted local theme, dark colour scheme) and was restored to light. A temporary daily-quote bookmark appeared in the authenticated member’s private Saved index, then was removed; the index refreshed to its empty state. The daily field note is selected deterministically from the local calendar date without a timer, external request, or background job. Desktop and 375px homepage/Saved-index checks were completed, with the saved-index capture’s transient loading state independently confirmed as a guest-session timing artifact rather than private-content exposure.
