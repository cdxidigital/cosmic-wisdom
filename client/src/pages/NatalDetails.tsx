import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, BookOpen, LockKeyhole } from "lucide-react";
import { Link } from "wouter";

function count(value: unknown) { return Array.isArray(value) ? value.length : 0; }

export default function NatalDetails() {
  const { isAuthenticated } = useAuth();
  const chart = trpc.cosmic.getNatalChart.useQuery(undefined, { enabled: isAuthenticated });
  const data = chart.data?.chartData as { planets?: unknown[]; houses?: unknown[]; aspects?: unknown[] } | undefined;
  return <main className="min-h-screen bg-[#F3F0E9] text-[#102936]"><header className="border-b border-[#102936]/10"><div className="mx-auto flex max-w-4xl items-center justify-between px-5 py-5"><Link href="/" className="flex items-center gap-3 font-mono text-[10px] tracking-[.14em]"><ArrowLeft size={14} /> COSMIC WISDOM</Link><span className="font-mono text-[9px] tracking-[.14em] text-[#B63C5E]">NATAL / METHOD NOTES</span></div></header><section className="mx-auto max-w-4xl px-5 py-14"><p className="font-mono text-[10px] font-semibold tracking-[.16em] text-[#B63C5E]">CHART DETAILS</p><h1 className="mt-5 font-serif text-6xl leading-[.85] tracking-[-.065em]">See what your reading is built from.</h1><p className="mt-6 max-w-2xl font-sans text-[16px] leading-7 text-[#55707d]">Cosmic keeps your chart private and turns your saved birth details into placements, houses, and aspects for reflective interpretation. This is a method note, not a prediction.</p>{!isAuthenticated ? <div className="mt-10 border border-[#102936]/15 bg-white/60 p-7"><LockKeyhole className="text-[#B63C5E]" size={18} /><h2 className="mt-4 font-serif text-3xl">Your chart details are private.</h2><p className="mt-3 font-sans text-[14px] leading-6 text-[#55707d]">Sign in to see the calculation notes associated with your own natal chart.</p><Button onClick={startLogin} className="mt-6 rounded-none bg-[#211B2A] font-mono text-[9px] tracking-[.14em] text-white">SIGN IN</Button></div> : chart.data ? <div className="mt-10 space-y-5"><div className="grid gap-4 sm:grid-cols-3">{[["PLACEMENTS", count(data?.planets)],["HOUSES", count(data?.houses)],["ASPECTS", count(data?.aspects)]].map(([label, value]) => <div key={String(label)} className="border border-[#102936]/15 bg-white/60 p-5"><p className="font-mono text-[9px] tracking-[.14em] text-[#B63C5E]">{label}</p><p className="mt-3 font-serif text-4xl">{value}</p></div>)}</div><details className="border border-[#102936]/15 bg-white/60 p-6"><summary className="cursor-pointer list-none font-mono text-[10px] font-semibold tracking-[.14em]"><span className="inline-flex items-center gap-2"><BookOpen size={14} className="text-[#B63C5E]" /> CALCULATION NOTES</span></summary><div className="mt-5 border-t border-[#102936]/10 pt-5 font-sans text-[14px] leading-7 text-[#55707d]"><p>Your chart is calculated from the birth date, time, and place you saved to your private profile. It includes planet placements, houses, and major aspects; the written reading is a reflective interpretation of those calculated inputs.</p><p className="mt-4">You can recalculate whenever you update your profile. Cosmic does not present the chart as a diagnosis, guarantee, or substitute for professional advice.</p></div></details></div> : <div className="mt-10 border border-[#102936]/15 bg-white/60 p-7"><p className="font-serif text-3xl">No calculated chart yet.</p><p className="mt-3 font-sans text-[14px] leading-6 text-[#55707d]">Create your profile and choose Calculate My Chart from the natal section when you are ready.</p><Link href="/" className="mt-6 inline-block font-mono text-[10px] tracking-[.14em] text-[#B63C5E]">RETURN TO YOUR HOME →</Link></div>}</section></main>;
}
